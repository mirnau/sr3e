# Issue Tracker

**Tracker:** GitHub Issues  
**Repo:** mirnau/sr3e  
**CLI:** `gh`

## Rules for agents

- Create issues with `gh issue create`
- List issues with `gh issue list`
- When referencing an issue in a commit, use `#<number>`
- Do not create duplicate issues — search first with `gh issue list --search "<keywords>"`
