export class SR3EBroadcaster extends Actor {
    prepareData() {
        super.prepareData();
    }

    prepareBaseData() {
        // Initialize or reset base data here
    }

    prepareDerivedData() {
        const systemData = this.system;
        // Compute derived data here
    }
}