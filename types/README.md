# Types

This folder holds ambient type definitions used by the project. Foundry VTT
types are brought in via `types/foundry.d.ts`, which references the installed
`@league-of-foundry-developers/foundry-vtt-types` package. Add any local
augmentations or shims as additional `.d.ts` files here.
