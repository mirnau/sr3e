---
phase: 01-character-creation-foundation
plan: 02
type: execute
---

<objective>
Create the CharacterCreationService coordinator singleton and Foundry ApplicationV2 wrapper.

Purpose: Build the service that orchestrates the repositories and business logic from Plan 1, plus the Foundry integration layer that launches the character creation dialog. Follows the established NewsService pattern for singleton composition.

Output: CharacterCreationService singleton and CharacterCreationApp Foundry wrapper, ready for Svelte UI integration in Plan 3.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Completed in Plan 1:**
@.planning/phases/01-character-creation-foundation/01-01-SUMMARY.md

**Services from Plan 1:**
@module/services/character-creation/MetatypeRepository.ts
@module/services/character-creation/MagicRepository.ts
@module/services/character-creation/PriorityValidator.ts
@module/services/character-creation/CharacterInitializer.ts

**Reference implementation:**
@C:\Users\mikae\AppData\Local\FoundryVTT\Data\systems\old_project\sr3e\module\foundry\applications\CharacterCreationApp.js
@C:\Users\mikae\AppData\Local\FoundryVTT\Data\systems\old_project\sr3e\module\services\CharacterGeneratorService.js
@C:\Users\mikae\AppData\Local\FoundryVTT\Data\systems\old_project\sr3e\module\services\ActorDataService.js

**Established patterns:**
@module/services/news-service/NewsService.svelte.ts

**Prior decisions:**
- Singleton pattern with static Instance getter
- Services compose collaborators via constructor injection
- No `any` types - strict TypeScript
- Character creation uses direct Foundry API (not StoreManager - one-time initialization)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CharacterCreationService singleton coordinator</name>
  <files>module/services/character-creation/CharacterCreationService.ts</files>
  <action>
Create CharacterCreationService as a singleton that coordinates the four services from Plan 1.

Follow NewsService.svelte.ts pattern:
- Private static instance field
- Static Instance() getter that creates singleton on first access
- Private constructor that instantiates collaborators
- Collaborators as private readonly fields

Compose these services:
- `#metatypeRepo: MetatypeRepository`
- `#magicRepo: MagicRepository`
- `#validator: PriorityValidator`
- `#initializer: CharacterInitializer`

Public methods to expose:
- `getMetatypes()` → delegates to metatypeRepo.getAllMetatypes()
- `getMagics()` → delegates to magicRepo.getAllMagics()
- `validatePriorities(selections)` → delegates to validator
- `initializeCharacter(actor, selections)` → delegates to initializer
- `generateRandomPriorities(metatypeOptions, magicOptions)` → weighted randomization algorithm from CharacterGeneratorService.js

Randomization algorithm (from old code):
- Uses weighted priorities: metatype {E: 64, C: 18, D: 18}, magic {A: 2, B: 2, C: 32, D: 32, E: 32}
- Draws from remaining priorities ensuring no duplicates
- If only one option for metatype/magic, forces that priority
- Returns {metatype, magic, attribute, skills, resources} priorities

Type all method parameters and returns - no `any` types.
Include static readonly VALID_PRIORITIES = ["A", "B", "C", "D", "E"] constant.
  </action>
  <verify>
- File exists at module/services/character-creation/CharacterCreationService.ts
- TypeScript compiles: npx tsc --noEmit
- No `any` types used
- Singleton pattern correctly implemented (private constructor, static Instance getter)
- All four services from Plan 1 imported and composed
  </verify>
  <done>CharacterCreationService singleton created, coordinates all services with proper typing and weighted randomization</done>
</task>

<task type="auto">
  <name>Task 2: Create CharacterCreationApp Foundry wrapper</name>
  <files>module/sheets/actors/CharacterCreationApp.ts</files>
  <action>
Port CharacterCreationApp from JS to TypeScript as a Foundry ApplicationV2 wrapper.

Extend: `foundry.applications.api.ApplicationV2`

Follow the pattern from CharacterCreationApp.js:
- Private fields for actor, callbacks, Svelte app instance
- Constructor accepts actor and options {onSubmit, onCancel}
- Static DEFAULT_OPTIONS with id, classes, window title, position settings
- _renderHTML() returns null (Svelte handles rendering)
- _replaceHTML(_, windowContent) mounts Svelte component (will be created in Plan 3)
- close() unmounts Svelte and calls onCancel if not submitted

Type the fields strictly:
- `#actor: Actor` (use proper Foundry Actor type)
- `#onSubmit: ((result: boolean) => void) | null`
- `#onCancel: (() => void) | null`
- `#svelteApp: any` (Svelte mount returns any - acceptable here ONLY for Svelte integration)
- `#wasSubmitted: boolean`

For _replaceHTML, prepare for Svelte mount (Plan 3 will complete):
- Import { mount, unmount } from "svelte"
- Mount will pass props: { actor, config: CONFIG.sr3e, onSubmit, onCancel }
- Store mounted app in #svelteApp for cleanup

Note: Svelte component doesn't exist yet - that's Plan 3. This plan sets up the wrapper.

Use mergeObject from foundry.utils for options merging.
Type all methods properly - no `any` except for #svelteApp field.
  </action>
  <verify>
- File exists at module/sheets/actors/CharacterCreationApp.ts
- TypeScript compiles: npx tsc --noEmit (will fail on Svelte component import - acceptable, Plan 3 creates it)
- Extends foundry.applications.api.ApplicationV2
- Proper types except #svelteApp field
- Follows pattern from old CharacterCreationApp.js
  </verify>
  <done>CharacterCreationApp wrapper created, ready for Svelte component integration in Plan 3</done>
</task>

<task type="auto">
  <name>Task 3: Create priority tables and helper types</name>
  <files>module/services/character-creation/types.ts</files>
  <action>
Create a types file with shared TypeScript interfaces and constants for character creation.

Constants (from ActorDataService.getCharacterCreationStats):
```typescript
export const PRIORITY_TABLES = {
  attributes: [
    { priority: "A", points: 30 },
    { priority: "B", points: 27 },
    { priority: "C", points: 24 },
    { priority: "D", points: 21 },
    { priority: "E", points: 18 },
  ],
  skills: [
    { priority: "A", points: 50 },
    { priority: "B", points: 40 },
    { priority: "C", points: 34 },
    { priority: "D", points: 30 },
    { priority: "E", points: 27 },
  ],
  resources: [
    { priority: "A", points: 1000000 },
    { priority: "B", points: 400000 },
    { priority: "C", points: 90000 },
    { priority: "D", points: 20000 },
    { priority: "E", points: 5000 },
  ],
} as const;

export const AGE_PHASES = [
  { text: "child", from: 0.0, to: 0.15 },
  { text: "adolescent", from: 0.15, to: 0.2 },
  { text: "youngadult", from: 0.2, to: 0.4 },
  { text: "adult", from: 0.4, to: 0.5 },
  { text: "middleage", from: 0.5, to: 0.65 },
  { text: "goldenyears", from: 0.65, to: 0.8 },
  { text: "ancient", from: 0.8, to: 1.0 },
] as const;
```

Interfaces:
- PrioritySelection: {metatype, magic, attribute, skill, resource}
- CharacterCreationSelections: extends PrioritySelection with {age, height, weight}
- MetatypeOption, MagicOption, PriorityTableEntry
- Any other shared types needed across services

Use proper TypeScript - `as const` for constants, readonly where appropriate, no `any`.
  </action>
  <verify>
- File exists at module/services/character-creation/types.ts
- TypeScript compiles: npx tsc --noEmit
- Constants match SR3e rules from old code
- All interfaces properly typed
- No `any` types
  </verify>
  <done>Shared types and constants created, SR3e priority tables correctly defined</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] CharacterCreationService singleton created and coordinates all services
- [ ] CharacterCreationApp wrapper created (may not compile until Plan 3 Svelte component exists)
- [ ] Shared types file with SR3e constants created
- [ ] No `any` types except #svelteApp field in wrapper
- [ ] Singleton pattern follows NewsService pattern
</verification>

<success_criteria>
- All tasks completed
- CharacterCreationService coordinates all four services from Plan 1
- CharacterCreationApp wrapper ready for Svelte mount
- Shared types and SR3e constants properly defined
- Code follows established singleton pattern
- TypeScript strict mode (except pending Svelte component)
</success_criteria>

<output>
After completion, create `.planning/phases/01-character-creation-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Character Creation Service Summary

**[One-liner about what shipped]**

## Accomplishments

- Created CharacterCreationService singleton coordinator
- Created CharacterCreationApp Foundry ApplicationV2 wrapper
- Defined shared types and SR3e priority tables

## Files Created/Modified

- `module/services/character-creation/CharacterCreationService.ts` - Singleton coordinator
- `module/sheets/actors/CharacterCreationApp.ts` - Foundry wrapper for Svelte UI
- `module/services/character-creation/types.ts` - Shared interfaces and constants

## Decisions Made

[Document any implementation decisions, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 01-03-PLAN.md - Svelte component refactor with UI integration
</output>
