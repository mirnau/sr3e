---
phase: 3.1-svelte-component-debloat
plan: 04
type: execute
---

<objective>
Debloat remaining Svelte files: item editors, common components, dialogs, and injections. Final TypeScript build verification.

Purpose: Complete the sweep of all 38 Svelte files. Item editors (skill editors) had localize fallbacks removed in the previous session — check for remaining anti-patterns. Common components and dialogs need a scan-and-fix pass.
Output: All 38 Svelte files clean. TypeScript build passes. Phase 3.1 complete.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-phase.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

**Reference implementation (clean pattern):**
@module/ui/actors/actor-components/DicePools.svelte

**Prior plan summary:**
@.planning/phases/3.1-svelte-component-debloat/3.1-03-SUMMARY.md

**Files to fix (item editors — localize fallbacks already removed):**
@module/ui/items/skill-editor/ActiveSkillEditorApp.svelte
@module/ui/items/skill-editor/KnowledgeSkillEditorApp.svelte
@module/ui/items/skill-editor/LanguageSkillEditorApp.svelte
@module/ui/items/MetatypeApp.svelte
@module/ui/items/SkillApp.svelte
@module/ui/items/skill-editor/SpecializationCard.svelte

**Remaining files to scan:**
@module/ui/common-components/CreationPointList.svelte
@module/ui/common-components/Image.svelte
@module/ui/common-components/ItemSheetWrapper.svelte
@module/ui/common-components/JournalViewer.svelte
@module/ui/common-components/JournalViewerToolbar.svelte
@module/ui/common-components/StatCard.svelte
@module/ui/common-components/PackeryGrid.svelte
@module/ui/common-components/SheetCard.svelte
@module/ui/common-components/ItemSheetComponent.svelte
@module/ui/dialogs/JournalSearchModal.svelte
@module/ui/dialogs/CharacterCreationDialog.svelte
@module/ui/injections/NewsFeed.svelte
@module/ui/injections/NeonName.svelte
@module/ui/actors/BroadcasterApp.svelte

**Rules to enforce:**
1. Stores are top-level `const` — never in `$effect` or `$state`
2. `storeManager.Subscribe(x)` at top level; `onDestroy` for Unsubscribe
3. No local `localize()` wrapper — import from `services/utilities`
4. No `?? fallback` after dereferenced stores
5. Required props where always provided
6. No localize fallbacks
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix item editors</name>
  <files>
    module/ui/items/skill-editor/ActiveSkillEditorApp.svelte,
    module/ui/items/skill-editor/KnowledgeSkillEditorApp.svelte,
    module/ui/items/skill-editor/LanguageSkillEditorApp.svelte,
    module/ui/items/MetatypeApp.svelte,
    module/ui/items/SkillApp.svelte,
    module/ui/items/skill-editor/SpecializationCard.svelte
  </files>
  <action>
Read each file and apply all rules. These files had localize fallbacks removed in the previous session. Check for remaining anti-patterns.

**For each file, check and fix:**

1. **$state+$effect store pattern**: Look for `let x = $state<Writable<...> | null>` assigned inside `$effect`. If found, move stores to top-level `const`.

2. **Local localize wrapper**: Look for `function localize(key: string) { return game.i18n.localize(key); }`. Remove and add `import { localize } from ...` if not already imported.

3. **?? after store dereferences**: Remove `$storeName ?? 0` / `$storeName ?? ""` / `$storeName ?? false` where the store provides a default value. Keep `??` for non-store optional chain access (e.g., `item.system?.field ?? 0`).

4. **Nullable props**: Fix `actor = null` defaults to required props if actor is always passed.

5. **`storeManager.Subscribe`/`Unsubscribe` placement**: Verify Subscribe is at top level and Unsubscribe is in `onDestroy`.

**SpecializationCard.svelte** — already clean (no StoreManager). Has `$effect` syncing `liveText` from `specialization.name` prop — this is legitimate prop-sync pattern. Leave it.

For each file, only fix what's actually wrong. Do not refactor code that is already correct.
  </action>
  <verify>All 6 files compile without TypeScript errors. No local localize wrappers. No $state+$effect store patterns.</verify>
  <done>
  - All item editors pass rules check
  - Any found anti-patterns fixed
  - SpecializationCard unchanged (already clean)
  </done>
</task>

<task type="auto">
  <name>Task 2: Scan and fix common components, dialogs, injections, and remaining files</name>
  <files>
    module/ui/common-components/CreationPointList.svelte,
    module/ui/common-components/Image.svelte,
    module/ui/common-components/ItemSheetWrapper.svelte,
    module/ui/common-components/JournalViewer.svelte,
    module/ui/common-components/JournalViewerToolbar.svelte,
    module/ui/common-components/StatCard.svelte,
    module/ui/common-components/PackeryGrid.svelte,
    module/ui/common-components/SheetCard.svelte,
    module/ui/common-components/ItemSheetComponent.svelte,
    module/ui/dialogs/JournalSearchModal.svelte,
    module/ui/dialogs/CharacterCreationDialog.svelte,
    module/ui/injections/NewsFeed.svelte,
    module/ui/injections/NeonName.svelte,
    module/ui/actors/BroadcasterApp.svelte
  </files>
  <action>
Read each file and apply all rules. Most of these files are expected to be clean (StatCard, Image, PackeryGrid, SheetCard, CharacterCreationDialog are likely pure presentational components). Apply the checklist:

For each file:
1. Does it use `StoreManager`? → Check store initialization pattern
2. Does it define a local `localize()` function? → Remove and import
3. Does it have `?? fallback` after `$store` dereferences? → Remove
4. Does it have `$state` + `$effect` store pattern? → Move to top-level
5. Does it have `storeManager.Unsubscribe` in `$effect` return? → Move to `onDestroy`
6. Does it have nullable actor/item props that are always provided? → Make required

**CharacterCreationDialog.svelte**: This is a dialog component — likely uses direct Foundry API for one-time operations (not continuous store sync). Per PROJECT.md decision: "Data Access for Dialogs — Direct Foundry API for one-time operations." If it uses direct API calls rather than StoreManager, that's intentional. Don't add StoreManager if it's not there.

**NewsFeed.svelte** and **NeonName.svelte**: These are UI injections likely using NewsService or similar. Apply rules but respect their architecture.

Only fix what's actually wrong. Skip files that are already clean.
  </action>
  <verify>All files scanned. Any found anti-patterns fixed. Files that were already clean are unchanged.</verify>
  <done>
  - All 14 files scanned
  - Anti-patterns fixed where found
  - Clean files left unchanged
  - List of changes documented in summary
  </done>
</task>

<task type="auto">
  <name>Task 3: TypeScript build verification</name>
  <files>No file changes — verification only</files>
  <action>
Run the TypeScript compiler to verify no type errors were introduced across all 4 plans of this phase.

```bash
cd "C:/Users/mikae/AppData/Local/FoundryVTT/Data/systems/sr3e"
npx tsc --noEmit
```

If there are TypeScript errors:
- Read the error messages
- Fix type errors in the relevant files (typically: `Actor` not recognized as a type — check that the Foundry type augmentation is available, or use the existing type patterns from DicePools.svelte as reference)
- Re-run until clean

Common issues to watch for:
- `Actor` type not available → use the same type import pattern as DicePools.svelte or CharacterSheetApp.svelte
- `Writable` type import removed but still referenced somewhere
- Store type inference issues after removing `$state<Writable<T> | null>` annotations
  </action>
  <verify>`npx tsc --noEmit` exits with code 0 (no errors).</verify>
  <done>
  TypeScript compiler reports no errors. All 38 Svelte files are clean and type-safe.
  </done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] All 38 Svelte files have been touched or confirmed clean
- [ ] No $state+$effect store initialization pattern in any file
- [ ] No local `localize()` wrapper functions in any file
- [ ] No manual Foundry hook registrations for data handled by StoreManager
- [ ] No `?? fallback` after dereferenced stores (`$storeName`)
- [ ] `npx tsc --noEmit` passes with no errors
</verification>

<success_criteria>
- All tasks completed
- TypeScript build passes
- Phase 3.1 complete — all 38 Svelte files normalized to DicePools.svelte patterns
</success_criteria>

<output>
After completion, create `.planning/phases/3.1-svelte-component-debloat/3.1-04-SUMMARY.md`:

# Phase 3.1 Plan 04: Item Editors + Remaining Files Summary

**[One-liner: what was fixed and that phase 3.1 is complete]**

## Accomplishments

## Files Modified
[List each file changed]

## Files Confirmed Clean (No Changes Needed)
[List files that were scanned and already compliant]

## Decisions Made

## Issues Encountered

## Next Step
Phase 3.1 complete. Ready for Phase 4+ planning (Skills System).
</output>
