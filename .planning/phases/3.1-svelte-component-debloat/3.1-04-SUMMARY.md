# Phase 3.1 Plan 04: Item Editors + Remaining Files Summary

Fixed the final set of Svelte anti-patterns across item editors and common components, then cleared all pre-existing TypeScript build errors. Phase 3.1 is complete — all 38 Svelte files normalized to the DicePools.svelte pattern.

## Accomplishments

- Task 1: Audited all 6 item editor files. Fixed `$state(item.system)` anti-pattern in MetatypeApp.svelte. All skill editor files (ActiveSkillEditorApp, KnowledgeSkillEditorApp, LanguageSkillEditorApp) were already clean. SpecializationCard's `$effect` for `liveText` prop-sync left intentionally intact.
- Task 2: Scanned all 14 common component / dialog / injection files. Fixed `$state+$effect` store initialization pattern in NeonName.svelte and BroadcasterApp.svelte (stores moved to top-level const, Subscribe/Unsubscribe moved to top-level/onDestroy). All other files were already clean.
- Task 3: TypeScript build — fixed 14 pre-existing errors across 9 non-Svelte files: DataModel→TypeDataModel in 8 item model files, typed `item` callback parameter in AttributeSpendingService, prefixed unused `actor` param in SkillSpendingService, removed unused canvas field declarations in CardiogramAnimationService, removed erroneous `private` keyword from `#` field in BroadcastRegistry, removed invalid `override` from `_onDropItem` in CharacterSheet. Build now passes clean.

## Files Modified

**Svelte files (anti-pattern fixes):**
- `module/ui/items/MetatypeApp.svelte` — removed `$state()` wrapper from `item.system` reference
- `module/ui/injections/NeonName.svelte` — moved store to top-level const, replaced `$effect` with `onDestroy`
- `module/ui/actors/BroadcasterApp.svelte` — moved 3 stores to top-level const, replaced `$effect` init with `onDestroy`

**TypeScript files (build errors fixed):**
- `module/models/items/item-components/Commodity.ts` — DataModel → TypeDataModel
- `module/models/items/item-components/CustomToken.ts` — DataModel → TypeDataModel
- `module/models/items/item-components/Portability.ts` — DataModel → TypeDataModel
- `module/models/items/item-components/RangeBand.ts` — DataModel → TypeDataModel
- `module/models/items/item-components/RollData.ts` — DataModel → TypeDataModel
- `module/models/items/item-components/SkillSpecialization.ts` — DataModel → TypeDataModel
- `module/models/items/MechanicalModel.ts` — DataModel → TypeDataModel
- `module/models/items/TechInterfaceModel.ts` — DataModel → TypeDataModel
- `module/services/character-creation/AttributeSpendingService.ts` — typed `item` callback parameter
- `module/services/character-creation/SkillSpendingService.ts` — prefixed unused `actor` param with `_`
- `module/services/health/CardiogramAnimationService.ts` — removed unused canvas field declarations
- `module/services/news-service/BroadcastRegistry.ts` — removed `private` from `#` field
- `module/sheets/actors/CharacterSheet.ts` — removed invalid `override` modifier

**Previously accumulated working-tree changes (from earlier sessions in this phase):**
- `module/ui/actors/actor-components/DicePools.svelte` — full debloat (store anti-patterns removed)
- `module/ui/actors/actor-components/Karma.svelte` — debloated
- `module/ui/actors/actor-components/Movement.svelte` — debloated
- `module/ui/actors/actor-components/StatCard.svelte` — formatting
- `module/ui/actors/injections/CharacterCreationManager.svelte` — formatting
- `module/ui/actors/injections/ShoppingCart.svelte` — formatting
- `module/ui/actors/CharacterSheetApp.svelte` — removed unused `form` prop
- `module/ui/common-components/*.svelte` — formatting/whitespace normalization
- `module/ui/dialogs/*.svelte` — formatting/whitespace normalization
- `module/ui/items/SkillApp.svelte` — formatting, removed `?? fallback` after localize calls
- `module/ui/items/skill-editor/*.svelte` — formatting, comment cleanup
- `module/documents/SR3EActor.ts` — newline fix
- `.planning/PROJECT.md` — added "No Localize Fallbacks" constraint

## Files Confirmed Clean (No Changes Needed)

- `module/ui/items/skill-editor/ActiveSkillEditorApp.svelte` — stores at top-level, onDestroy unsubscribe, localize imported
- `module/ui/items/skill-editor/KnowledgeSkillEditorApp.svelte` — same as above
- `module/ui/items/skill-editor/LanguageSkillEditorApp.svelte` — same as above
- `module/ui/items/skill-editor/SpecializationCard.svelte` — no StoreManager; `$effect` for liveText is legitimate prop-sync
- `module/ui/common-components/CreationPointList.svelte` — pure presentational, no stores
- `module/ui/common-components/Image.svelte` — clean
- `module/ui/common-components/ItemSheetWrapper.svelte` — pure presentational
- `module/ui/common-components/JournalViewer.svelte` — `$state` for local UI state only (journalId), legitimate
- `module/ui/common-components/JournalViewerToolbar.svelte` — local UI state only
- `module/ui/common-components/StatCard.svelte` — `game.i18n.localize()` called directly (not a wrapper), clean
- `module/ui/common-components/PackeryGrid.svelte` — clean
- `module/ui/common-components/SheetCard.svelte` — pure presentational
- `module/ui/common-components/ItemSheetComponent.svelte` — pure presentational
- `module/ui/dialogs/JournalSearchModal.svelte` — `$state` for local UI state, legitimate
- `module/ui/dialogs/CharacterCreationDialog.svelte` — correct: direct Foundry API per project decision for dialogs
- `module/ui/injections/NewsFeed.svelte` — `currentDisplayFrame.subscribe` in `onMount` with cleanup, correct pattern for news service

## Decisions Made

- `CardiogramAnimationService.ts`: removed `lineCanvas`/`pointCanvas` private field declarations (fields assigned in constructor but never read — only their derived `ctx` and dimension values are used)
- `SkillSpendingService.canDecrease`: prefixed unused `actor` param with `_` to preserve public API signature while satisfying `noUnusedLocals`
- Pre-existing working-tree changes from prior sessions included in this commit — they are all part of phase 3.1 debloat work

## Issues Encountered

- TypeScript errors were all pre-existing (present in committed HEAD), not introduced by this plan's changes — confirmed by stashing and re-running tsc
- `private #field` combination is invalid TypeScript — ES private fields (`#`) are inherently private; adding the `private` modifier is redundant and an error

## Next Step

Phase 3.1 complete. Ready for Phase 4+ planning (Skills System).
