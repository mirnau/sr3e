# Phase 2.4 Plan 02: SkillSpendingService Extraction Summary

**SkillSpendingService activated as single source of truth for all skill buying/selling/delete; editors are now presentational-only**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-03-13
- **Completed:** 2026-03-13
- **Tasks:** 2 auto + 1 human-verify
- **Files modified:** 4

## Accomplishments

- SkillSpendingService fully rewritten: correct Intelligence-linked formula for all skill types, store-only reads (`get(store)`), store-only writes (`store.set()`), delete-first-then-refund pattern, specialization-aware refund
- All three editors delegate to service — no inline point arithmetic remains in any Svelte file
- `SkillCategory` type exported from service and imported by editors (explicit category param, no `skillType` field dependency)
- Knowledge/language editors pass actual Intelligence rating to service calls (not 0)

## Files Created/Modified

- `module/services/character-creation/SkillSpendingService.ts` — Full rewrite. Exports `SkillCategory` type. Five public methods (`canIncrease`, `increase`, `canDecrease`, `decrease`, `deleteWithRefund`). All skill types use same `value < linkedAttrRating ? 1 : 2` formula. No `actor.system` reads, no `document.update()`.
- `module/ui/items/skill-editor/ActiveSkillEditorApp.svelte` — Removed `activePoints` store and inline arithmetic; delegates to `SkillSpendingService` with `SKILL_CATEGORY = "active"`
- `module/ui/items/skill-editor/KnowledgeSkillEditorApp.svelte` — Removed `knowledgePoints` store and inline arithmetic; delegates to service with `SKILL_CATEGORY = "knowledge"` and Intelligence as `linkedAttrRating`
- `module/ui/items/skill-editor/LanguageSkillEditorApp.svelte` — Removed `languagePoints` store and inline arithmetic; delegates to service with `SKILL_CATEGORY = "language"` and Intelligence as `linkedAttrRating`

## Decisions Made

- **Uniform cost formula across all skill types** — service `#increaseCost` and `#decreaseRefund` use `currentValue < linkedAttrRating ? 1 : 2` for all categories; callers are responsible for passing the correct threshold (active = linked attr, knowledge/language = Intelligence)
- **`SkillCategory` exported from service** — avoids re-declaration in editors and ensures type consistency

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Service used flat 1 CP for knowledge/language; editors passed linkedAttrRating = 0**

- **Found during:** Post-subagent review of generated code
- **Issue:** Subagent implemented `#increaseCost` with `if (category !== "active") return 1` — flat cost for knowledge/language. Also had `deleteWithRefund` use flat 1 per level for non-active. Editors passed `0` as `linkedAttrRating` to service calls (per original plan text that was not fully updated after rules correction).
- **Fix:** Removed category-branching from `#increaseCost`, `#decreaseRefund`, and `deleteWithRefund` — uniform formula for all types. Added `linkedAttrRating` (hardcoded Intelligence) back to knowledge/language editors and wired to all service calls.
- **Files modified:** SkillSpendingService.ts, KnowledgeSkillEditorApp.svelte, LanguageSkillEditorApp.svelte
- **Verification:** User approved at checkpoint

---

**Total deviations:** 1 auto-fixed (stale plan text caused wrong formula in service)
**Impact on plan:** Necessary rules correction. No scope creep.

## Issues Encountered

None.

## Next Phase Readiness

- Phase 2.4 complete — all buying mechanics comply with SR3e rules
- SkillSpendingService is the single write path for all skill spending
- All editors are presentational-only
- Ready for Phase 2.2 (Health Component Visual Polish) or Phase 3 (Karma & Experience Core)

---
*Phase: 2.4-buying-mechanics-overhaul*
*Completed: 2026-03-13*
